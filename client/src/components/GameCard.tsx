import React from 'react'
import { GameItem } from '../model'

interface Props {
    game: GameItem
}

export default function GameCard(props: Props) {
    return (
        <div>GameCard</div>
    )
}
